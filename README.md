# SEO-GPT-No_API
Built an offline SEO/AEO chatbot using a local LLM via Ollama. Implemented a RAG pipeline: chunked SEO documents, embedded with sentence-transformer, stored vectors in ChromaDB. At query time retrieve top chunks and send them with system prompts and chat history to the model. Streamlit UI includes chat memory and source display. with citations. v1.
